# Toralizer In C

- Prerequisites
- Make sure you have tor server running locally on your server 
- host : 127.0.0.1  (Default)
- PORT : 9050       (Default)

### Run the code
```
$ make

$ ./toralize HOST PORT
```